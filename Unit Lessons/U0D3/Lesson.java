public class Lesson{

    public static void main(String [] args){
        // variables
        int number = 5; // assignment opperator

        // single = assigns a value
        //different from ==, which compares a value

        //different assignment operators
        number += 3; // this is the same as number = number + 6
        number -= 3; // number = number - 3
        number *= 10; // number = number * 10
        number /= 2; //number = number /2

        // increment and decrement operators
        number += 1; //(this adds one)
        number ++; //(this also adds one!)

        number -=1;
        number --;

        //boolean operators, evaluate to true or false
        //use them in if statements
        /* 
        number == 5; //equals
        number != 1 // not equals
        number <= 15: //les than or equal to
        number >= // greater than or equal to
        */
    
            
}